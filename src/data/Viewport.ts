export interface Viewport {
    /**
     * The X coordinate the viewport is centered on
     */
    centerX: number
    /**
     * The Y coordinate the viewport is centered on
     */
    centerY: number
    /**
     * The zoom level (scale of nodes & edges container)
     */
    zoom: number
}